import React from 'react';

import resume from '../assets/ResumeImage.png'

const Resume = () => {
    return (
        <section id="resume">
        <h2>Resume</h2>
        <img src={resume}/>
    </section>
    );
};

export default Resume;